Du übernimmst einen bereits teilweise bearbeiteten, aber **nicht vertrauenswürdig abgenommenen** Implementierungsstand.

Der vorherige Agent hat mehrfach einen vollständigen Abschluss gemeldet, obwohl anschließend zentrale fehlende beziehungsweise fehlerhafte Produktionsverdrahtungen gefunden wurden. Deshalb gilt der vorhandene Stand ausschließlich als:

**UNTRUSTED PARTIAL IMPLEMENTATION**

Du sollst die vorhandene Arbeit nicht pauschal verwerfen, aber auch **nichts ungeprüft als korrekt übernehmen**.

# 1. Arbeitsbereiche

## Eingefrorener Quellstand des vorherigen Agenten

```text
P:\GithubRepos\marcosudau-vps-worktrees\einheitliche-triggerarchitektur
```

Dieser gesamte Pfad ist für dich **READ ONLY**.

Dort darfst du absolut nichts verändern.

## Dein eigener Arbeitsbereich

```text
P:\GithubRepos\marcosudau-vps-worktrees\einheitliche-triggerarchitektur-claude
```

Der Ordner ist derzeit leer.

Baue dir dort selbstständig deinen vollständigen Arbeitsbereich auf.

# 2. Zwingend zu übernehmende Repositories

Übernimm exakt diese drei Repositories mitsamt ihrem aktuellen relevanten **uncommitted und untracked Arbeitsstand**:

```text
voice-stt-server
voice-stt-client
led_controller_respeaker-v3
```

Quellen:

```text
P:\GithubRepos\marcosudau-vps-worktrees\einheitliche-triggerarchitektur\voice-stt-server

P:\GithubRepos\marcosudau-vps-worktrees\einheitliche-triggerarchitektur\voice-stt-client

P:\GithubRepos\marcosudau-vps-worktrees\einheitliche-triggerarchitektur\led_controller_respeaker-v3
```

Erzeuge in deinem Claude-Arbeitsbereich eigenständig bearbeitbare Checkouts auf exakt denselben Ausgangs-HEADs und übertrage anschließend die relevanten tracked und untracked Änderungen.

Kopiere nicht blind `.git`-Worktree-Metadaten.

Der ursprüngliche Antigravity-Arbeitsbereich muss vollständig unverändert bleiben.

# 3. Einzige zusätzliche fachliche Quelle

Lies aus dem bisherigen `.gemini`-Ordner ausschließlich diesen ursprünglichen, genehmigten Gesamtauftrag:

```text
P:\GithubRepos\marcosudau-vps-worktrees\einheitliche-triggerarchitektur\.gemini\001_Verbindlicher Ausführungsauftrag – Einheitliche serverseitige Triggerarchitektur.md
```

**Dieser vollständige Auftrag ist die normative Spezifikation.**

Lies keine anderen Gemini-/Antigravity-Prompts oder Planungsdateien aus `.gemini`.

Verwende auch die bisherigen Unterlagen unter:

```text
P:\GithubRepos\marcosudau-vps-worktrees\einheitliche-triggerarchitektur\zusammenarbeit
```

**nicht als Arbeitsgrundlage.**

Insbesondere keine bisherigen:

* `PLAN.md`
* `STATUS.md`
* `VALIDATION.md`
* `CONTRACTS.md`
* `DECISIONS.md`
* `REPORT.md`
* Gate-PASS-Tabellen
* Audit- oder Abschlussberichte

Diese enthalten teilweise inzwischen verworfene Bewertungen und sollen dein eigenes Architekturverständnis nicht beeinflussen.

Falls der normative Auftrag solche Dokumente verlangt, erstelle beziehungsweise pflege sie **neu in deinem eigenen Claude-Arbeitsbereich** anhand des tatsächlichen Codes und deiner eigenen Nachweise.

# 4. Vorgehen

Arbeite den ursprünglichen Gesamtauftrag vollständig von AP0 bis AP11 durch.

Für jedes Arbeitspaket:

1. Lies die vollständigen Anforderungen und Gate-Kriterien aus dem Originalauftrag.
2. Prüfe den tatsächlich vorhandenen Code und den übernommenen Working-Tree-Diff.
3. Klassifiziere vorhandene Arbeit als:

   * `NOT STARTED`
   * `PARTIAL`
   * `IMPLEMENTED BUT UNVERIFIED`
   * `VERIFIED`
4. Prüfe insbesondere, ob vorhandene Komponenten **im echten Produktionspfad tatsächlich aufgerufen und verdrahtet sind**.
5. Behalte korrekte vorhandene Arbeit.
6. Korrigiere oder ersetze falsche beziehungsweise unvollständige Arbeit.
7. Ergänze belastbare Tests.
8. Führe die vollständigen Gate-Kriterien des Originalauftrags aus.
9. Beginne erst nach bestandenem Gate mit dem nächsten AP.

Arbeite anschließend autonom bis zum vollständigen Abschluss weiter.

# 5. Wichtige Warnung aus dem bisherigen Verlauf

Es wurden bereits Fälle gefunden, bei denen:

* Methoden vorhanden und getestet waren, aber im Produktionscode nicht aufgerufen wurden;
* Servercallbacks Methodennamen aufriefen, die der `ActivationController` nicht besaß;
* Controlled-Konfiguration in Tests künstlich injiziert wurde, obwohl der reale Session-Admission-Pfad sie nicht korrekt verdrahtete;
* vermeintliche Timeout-/E2E-Tests Zustände direkt aus dem Test heraus auslösten.

Diese Beispiele sind **nur Warnungen und keine vollständige Fehlerliste**.

Prüfe den gesamten Auftrag selbstständig und suche nach weiteren Fällen dieser Art.

Eine grüne Gesamttestsuite allein ist ausdrücklich **kein ausreichender Gate-Nachweis**.

# 6. Besonders kritisch prüfen

Achte insbesondere auf die vollständige reale Verdrahtung von:

```text
eine Session
+ kontinuierlicher Audiostream
+ serverautoritativer ActivationController
+ Manual und Wake Word als Triggerquellen derselben Activation
+ gemeinsamer Recorder-/VAD-/Transkriptionspfad
```

Sowie auf:

* Client-Lifecycle und alte clientseitige Autoritäten
* `send_trigger()`
* `trigger_ack`
* `commandId`
* `activationId`
* `primarySource`
* `sources`
* Session Admission
* `manualTriggerEnabled`
* `wakeWordTriggerEnabled`
* Timeout/Scheduler/Generation
* Follow-up
* Finish/Cancel/Extend
* Reconnect
* Replay/Dedupe
* Triggerkollisionen
* Legacy-Kompatibilität
* Browserclient, soweit vom Originalauftrag verlangt
* reale E2E-Abnahme
* Frozen-/PyInstaller-Build
* Feedback/LEFX/ReSpeaker gemäß Originalauftrag

Bei:

```text
Manual → Wake Word
Wake Word → Manual
nahezu simultan
```

muss entsprechend dem Originalauftrag belastbar nachgewiesen werden, dass keine doppelte Activation beziehungsweise Ressourcenbelegung entsteht.

# 7. Reale Tests

Wenn der Originalauftrag reale Hardware- oder manuelle Tests verlangt und du diese nicht selbst durchführen kannst, markiere sie ausdrücklich als:

```text
MANUAL VALIDATION REQUIRED
```

und gib mir eine konkrete Testanweisung.

Behaupte niemals einen Hardware- oder realen E2E-PASS, wenn dieser Test nicht tatsächlich durchgeführt wurde.

# 8. ABSOLUTES GIT-VERBOT

Während des gesamten Auftrags:

**NICHT COMMITTEN.**

**NICHT PUSHEN.**

Zusätzlich verboten:

```text
git commit
git commit --amend
git push
git merge
git rebase
git tag
Pull Request erstellen
Remote-Refs verändern
```

Auch nach erfolgreichem Abschluss bleiben sämtliche Änderungen **uncommitted**.

Vor Beginn für alle drei Repositories Ausgangs-HEAD und `git status` dokumentieren.

Am Ende erneut:

```text
git rev-parse HEAD
git status --short
git diff --check
git diff --stat
git log -1 --oneline
```

Es muss gelten:

```text
INITIAL_HEAD == FINAL_HEAD
```

für alle drei Repositories.

Kontrolliere am Ende außerdem read-only, dass der ursprüngliche Antigravity-Arbeitsbereich gegenüber dem Start unverändert geblieben ist.

# 9. Recovery bei Limit

Wenn dein Claude-Nutzungslimit, Kontext oder Zeit knapp wird:

* keinen neuen tiefgreifenden Implementierungsschritt beginnen;
* aktuellen Zustand sauber sichern;
* Testsituation dokumentieren;
* Recovery-/Statusdokumentation aktualisieren;
* exakten nächsten technischen Schritt festhalten;
* erst dann stoppen.

# 10. Abschluss

Der Auftrag ist erst beendet, wenn der **vollständige ursprüngliche Ausführungsauftrag** technisch umgesetzt und nach dessen eigenen Gate-Kriterien validiert wurde.

Der finale Bericht muss klar unterscheiden zwischen:

* nachweisbar vollständig umgesetzt;
* übernommene und selbst verifizierte Vorarbeit;
* von dir korrigierte/ergänzte Bereiche;
* gefundene Test-/False-Positive-Lücken;
* reale E2E-Nachweise;
* manuelle Restabnahmen;
* offene Punkte;
* vollständige Regressionstests;
* Git-Status;
* Nachweis, dass kein Commit/Push erfolgte;
* Nachweis, dass der Antigravity-Quellstand unverändert blieb.

Geschwindigkeit ist ausdrücklich kein Ziel.

**Korrektheit, vollständiges Architekturverständnis und belastbare Evidence haben Vorrang.**

# 11. TOKENÖKONOMISCHE ARBEITSTEILUNG MIT OPENCODE / DEEPSEEK V4 FLASH

Nutze dein eigenes Claude-Kontext- und Tokenbudget gezielt für die Aufgaben, bei denen dein eigenes Architekturverständnis und deine eigene Entscheidung wirklich erforderlich sind.

Für **tokenintensive, aber gut delegierbare Arbeiten** sollst du bevorzugt über OpenCode das Modell **DeepSeek V4 Flash** als Hilfsagent einsetzen.

Geeignete delegierbare Aufgaben sind insbesondere:

* großflächige Code-Suchen;
* Aufrufer-/Referenzsuche über viele Dateien;
* Inventarisierung von Klassen, Methoden, Events, IDs und Configfeldern;
* Suche nach parallelen beziehungsweise Legacy-Codepfaden;
* Analyse umfangreicher Testdateien;
* Zuordnung vorhandener Tests zu Anforderungen;
* Diff-Auswertung;
* Dateistruktur- und Abhängigkeitsanalysen;
* Suche nach ungenutzten Methoden oder nur aus Tests aufgerufenen APIs;
* erste Rekonstruktion längerer Callflows;
* Vergleich wiederkehrender Implementierungsmuster;
* umfangreiche, mechanische Prüfungen, deren Ergebnis anschließend kompakt zusammengefasst werden kann.

DeepSeek V4 Flash soll dabei **nicht selbst die normative Gesamtarchitektur neu interpretieren oder endgültige Gate-Entscheidungen treffen**.

Die Verantwortung für:

* Architekturentscheidungen;
* Bewertung gegen den ursprünglichen normativen Auftrag;
* sicherheitskritische oder weitreichende Änderungen;
* Cross-Repository-Verträge;
* schwierige Race-/Lifecycle-Fragen;
* endgültige PASS-/FAIL-Entscheidungen;
* kritische Implementierungen;
* und die finale Integration

bleibt bei dir.

Das gewünschte Arbeitsmuster ist grundsätzlich:

```text
Claude identifiziert eine konkrete Untersuchungsfrage
        ↓
OpenCode / DeepSeek V4 Flash übernimmt die breite, tokenintensive Analyse
        ↓
DeepSeek liefert einen kompakten, konkreten Befund mit Dateien/Symbolen/Fundstellen
        ↓
Claude prüft die entscheidenden Fundstellen selbst
        ↓
Claude trifft die fachliche Entscheidung und implementiert beziehungsweise validiert
```

Delegiere also nicht einfach „prüfe das ganze Projekt“, sondern möglichst klar abgegrenzte Teilfragen, beispielsweise:

```text
Finde sämtliche Produktions-Aufrufer von STTSession.send_trigger(),
send_start() und send_stop() und ordne sie nach realem Runtime-Pfad.
Keine Architekturentscheidung treffen. Liefere nur Datei, Symbol,
Aufrufkontext und kurze Einordnung.
```

oder:

```text
Inventarisiere sämtliche Stellen, die ActivationController.expire()
direkt oder indirekt auslösen. Unterscheide Produktionscode und Tests.
```

oder:

```text
Prüfe alle Tests, die als E2E bezeichnet werden, darauf, ob sie
WebSocket-Entry, Query Parsing, Session Admission und reale Callbacks
durchlaufen oder Zustände künstlich injizieren.
```

## Wann Claude selbst arbeiten soll

Verwende dein eigenes Kontextbudget direkt, wenn:

* die Entscheidung mehrere Arbeitspakete oder Repositories gleichzeitig betrifft;
* ein Befund architektonisch mehrdeutig ist;
* eine falsche Entscheidung schwerwiegende Folgewirkungen hätte;
* der Originalauftrag interpretiert werden muss;
* ein Gate endgültig bewertet wird;
* eine zentrale Lifecycle-, Race-, Scheduler- oder Autoritätsfrage betroffen ist;
* du den von DeepSeek gelieferten Befund verifizieren musst.

## Wichtig

Delegation dient ausschließlich der **Token- und Kontextökonomie**, nicht dem Abschieben der Verantwortung.

Übernimm Ergebnisse von DeepSeek V4 Flash niemals ungeprüft als Wahrheit.

Bei allen wesentlichen Schlussfolgerungen gilt:

> DeepSeek darf suchen, inventarisieren, vergleichen und voranalysieren.
> Claude verifiziert die entscheidenden Belege und trifft die endgültige technische Entscheidung.

Nutze diese Arbeitsteilung konsequent, damit dein eigenes Kontextfenster möglichst lange für die wirklich schwierigen Teile des Gesamtauftrags erhalten bleibt.
